(()=>{const c=document.getElementById('field'),x=c.getContext('2d');let w=0,h=0,d=1,p=[];const R=()=>{d=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;c.width=w*d;c.height=h*d;c.style.width=w+'px';c.style.height=h+'px';x.setTransform(d,0,0,d,0,0);const n=Math.min(95,Math.max(42,Math.floor(w/18)));p=Array.from({length:n},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.16,vy:(Math.random()-.5)*.16,r:Math.random()*1.25+.35}))};const D=()=>{x.clearRect(0,0,w,h);for(let i=0;i<p.length;i++){const a=p[i];a.x+=a.vx;a.y+=a.vy;if(a.x<0||a.x>w)a.vx*=-1;if(a.y<0||a.y>h)a.vy*=-1;x.beginPath();x.arc(a.x,a.y,a.r,0,Math.PI*2);x.fillStyle='rgba(126,177,225,.28)';x.fill();for(let j=i+1;j<p.length;j++){const b=p[j],dx=a.x-b.x,dy=a.y-b.y,dd=Math.hypot(dx,dy);if(dd<115){x.beginPath();x.moveTo(a.x,a.y);x.lineTo(b.x,b.y);x.strokeStyle='rgba(94,155,201,'+((1-dd/115)*.055)+')';x.lineWidth=.7;x.stroke()}}}requestAnimationFrame(D)};addEventListener('resize',R,{passive:true});R();D();const g=document.querySelector('.cursor-glow');addEventListener('pointermove',e=>{g.style.left=e.clientX+'px';g.style.top=e.clientY+'px'},{passive:true});const items=[...document.querySelectorAll('.reveal')];items.forEach(e=>e.style.transitionDelay=(e.dataset.delay||0)+'ms');const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});items.forEach(e=>io.observe(e));document.querySelectorAll('.research-card,.project-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),xx=(e.clientX-r.left)/r.width-.5,yy=(e.clientY-r.top)/r.height-.5;card.style.transform='perspective(900px) rotateX('+(-yy*2.4)+'deg) rotateY('+(xx*2.4)+'deg) translateY(-4px)'});card.addEventListener('pointerleave',()=>card.style.transform='')});const progress=document.querySelector('.scroll-progress');const updateProgress=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max>0?(scrollY/max*100):0)+'%'};addEventListener('scroll',updateProgress,{passive:true});updateProgress();const lightbox=document.getElementById('lightbox'),lightImg=lightbox?.querySelector('img'),lightClose=lightbox?.querySelector('.lightbox-close');document.querySelectorAll('[data-lightbox]').forEach(btn=>btn.addEventListener('click',()=>{if(!lightbox)return;lightImg.src=btn.dataset.lightbox;lightImg.alt=btn.querySelector('img')?.alt||'Project artifact';lightbox.showModal()}));lightClose?.addEventListener('click',()=>lightbox.close());lightbox?.addEventListener('click',e=>{if(e.target===lightbox)lightbox.close()});})();

(()=>{const root=document.getElementById('recent-activity-list');if(!root)return;const raw=Array.isArray(window.RECENT_ACTIVITY)?window.RECENT_ACTIVITY:[],seen=new Set(),data=raw.filter(item=>{if(!item||!item.title||!item.date)return false;const key=String(item.id||item.date+'|'+item.title);if(seen.has(key))return false;seen.add(key);return true}).sort((a,b)=>String(b.date).localeCompare(String(a.date))).slice(0,6);const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));if(!data.length){root.innerHTML='<article class="activity-card activity-empty"><p>No verified activity published yet.</p></article>';return}root.innerHTML=data.map((item,i)=>{const body='<div class="activity-top"><span class="activity-date">'+esc(item.date)+'</span><span class="activity-category">'+esc(item.category||'BUILD')+'</span></div><h3>'+esc(item.title)+'</h3><p>'+esc(item.summary)+'</p><div class="activity-foot"><span>'+esc(item.status||'COMPLETED')+'</span><span>'+String(i+1).padStart(2,'0')+'</span></div>';return item.href?'<a class="activity-card reveal" href="'+esc(item.href)+'" target="_blank" rel="noreferrer">'+body+'</a>':'<article class="activity-card reveal">'+body+'</article>'}).join('');root.querySelectorAll('.reveal').forEach(e=>requestAnimationFrame(()=>e.classList.add('visible')))})();