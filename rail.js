(()=>{const links=[...document.querySelectorAll('[data-rail-target]')];if(!links.length)return;const sections=links.map(a=>document.getElementById(a.dataset.railTarget)).filter(Boolean),bar=document.getElementById('rail-progress'),pct=document.getElementById('rail-percent');const update=()=>{const max=Math.max(1,document.documentElement.scrollHeight-innerHeight),progress=Math.max(0,Math.min(1,scrollY/max));if(bar)bar.style.width=(progress*100).toFixed(1)+'%';if(pct)pct.textContent=String(Math.round(progress*100)).padStart(2,'0')+'%';const probe=scrollY+Math.min(innerHeight*.34,310);let current=sections[0]?.id;for(const sec of sections){if(sec.offsetTop<=probe)current=sec.id}for(const a of links){const active=a.dataset.railTarget===current;a.classList.toggle('active',active);if(active)a.setAttribute('aria-current','location');else a.removeAttribute('aria-current')}};addEventListener('scroll',update,{passive:true});addEventListener('resize',update,{passive:true});update()})();